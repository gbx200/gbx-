# 弘毅书院功能设计

> 本文结合《数据库 ORM 表设计.md》整理，主要描述微信端、PC 管理端、微信管理端、核心业务规则，以及当前功能与 ORM 的对应关系。后续可在此基础上继续细化接口、页面原型、权限点和字段规则。

---

## 1. 角色与入口

### 1.1 学生/教师微信端

学生和教师通过微信端进入系统。首次进入时，需要完成微信身份绑定。两类用户都可以报名活动、发布和互动广场内容、点赞收藏新闻以及预约自习室；信誉分、终身累计五育分和按学年累计的缺勤限制只对学生身份生效。

身份绑定规则：

- 系统通过微信 `openid` 识别当前微信用户。
- 首次登录时创建 `user_type="unbound"` 的用户，`username` 由系统随机生成 `wx_ + UUID`，仅用于兼容 Django 用户体系，不展示也不允许修改；账号启停统一使用 Django 的 `is_active`。
- 用户输入学号/工号，并通过校园统一认证、预置一次性绑定码或已登记手机号验证码完成身份校验；姓名只用于辅助确认，不作为唯一认证凭据。
- 学生身份与 `Student.student_no` 对应，教师身份与 `Teacher.employee_no` 对应。
- 校验通过后，将当前微信用户绑定到 `User.student` 或 `User.teacher`。
- 同一账号只能绑定学生或教师其中一种身份，绑定结果必须与 `User.user_type` 一致。
- 绑定失败需要限制连续尝试次数；解绑或换绑只能由有权限的管理员处理并记录操作日志。
- 绑定完成后，普通功能按身份开放，管理功能继续按角色和服务端权限开放。

微信端底部导航栏固定五个入口：

1. 首页
2. 活动
3. 广场
4. 消息
5. 我的

### 1.2 PC 管理端

PC 管理端用于基础数据维护、内容发布、“我的”tab 页广告管理、活动管理、预约管理、来信回复、广场审核、新闻发布和权限管理。

PC 管理端权限由 `Role`、`Menu` 和 Django `Permission` 控制。菜单只决定界面入口是否展示，接口操作必须校验 Django `Permission`，涉及具体收件方等业务时还要校验数据范围。

### 1.3 微信管理端

微信管理端面向具有管理职责的教师或工作人员，入口位于微信端“我的”页面中的“管理服务”区域。

微信管理端入口由 `Role` 和 `MenuWeixin` 控制，接口仍需校验服务端权限。系统根据当前用户绑定的角色动态展示可用管理服务，例如活动报名管理、会议室审批、信箱回复、树洞回复、自习室管理和宿舍管理。

---

## 2. 微信端功能设计

## 2.1 首页

首页用于聚合高频服务和重点内容，页面从上到下展示：

1. 书院服务头图
2. 常用服务入口
3. 活动推荐
4. 社区新闻

### 常用服务入口

首页展示六个快捷入口：

- 领导信箱
- 心灵树洞
- 自习室预约
- 会议室预约
- 书院简介
- 社区新闻

其中，自习室预约和领导信箱为重点入口

### 活动推荐

活动推荐只展示仍有参与价值的活动：

- 待报名：活动已发布，但尚未到报名开始时间。
- 报名中：当前时间处于报名时间范围内。

已结束、进行中、待开始且无法报名的活动不进入首页推荐。

### 社区新闻

社区新闻展示书院发布的重点新闻内容。列表卡片展示标题、分类、浏览量、点赞量、收藏量和发布时间，避免只展示数字导致含义不清。

对应 ORM：

- `Activity`
- `ActivityCategory`
- `News`
- `NewsCategory`

---

## 2.2 领导信箱

领导信箱用于学生向书院领导或相关部门反馈问题、提出建议。PC 管理端或微信管理端中具备权限的管理人员可以查看和回复来信。

### 列表页

列表页采用卡片形式，仅展示当前学生自己提交的信件。其他学生不能查看；具备处理权限的管理人员按收件方数据范围查看。

筛选项：

- 全部
- 未回复
- 已回复

卡片展示内容：

- 信件标题
- 内容摘要
- 提交时间
- 回复状态
- 收件领导/部门
- 回复未读提示

当信件已回复但学生尚未查看时，卡片右上角显示红点提示。学生进入详情页后，红点消失。

### 写信页

学生点击悬浮加号进入写信页。

表单内容：

- 标题
- 信件分类
- 收件领导/部门
- 正文内容
- 联系电话
- 是否匿名

提交后，信件进入“待回复”状态。

### 详情页

详情页展示信件原文、提交时间、处理状态和管理端回复内容。

对应 ORM：

- `LeaderMailboxCategory`
- `LeaderMailboxReceiver`
- `LeaderMailboxLetter`
- `User`
- `Student`

说明：ORM 中 `LeaderMailboxLetter` 已包含 `is_anonymous` 和 `student_read_at`，可支持匿名展示和回复红点。匿名时常规管理页面隐藏学生姓名、学号和联系方式，只有具备实名查看权限的人员可以查看，且需要记录查看日志。

---

## 2.3 心灵树洞

心灵树洞用于学生表达情绪、倾诉困惑或提交心理支持类内容。具备权限的教师或工作人员可在管理端进行回复。

### 列表页

列表页采用卡片形式，仅展示当前学生自己提交的树洞记录。其他学生不能查看；具备树洞处理权限的管理人员可以查看和回复。

筛选项：

- 全部
- 未回复
- 已回复

卡片展示内容：

- 标题
- 内容摘要
- 提交时间
- 回复状态
- 回复未读提示

当树洞内容已回复但学生尚未查看时，卡片右上角显示红点提示。学生进入详情页后，红点消失。

### 写信页

学生点击悬浮加号进入提交页。

表单内容：

- 标题
- 正文内容
- 联系电话
- 是否匿名展示

提交后，内容进入“待回复”状态。

### 详情页

详情页展示学生原文、提交时间、处理状态和管理端回复内容。

对应 ORM：

- `TreeHoleLetter`
- `User`
- `Student`

说明：ORM 中 `TreeHoleLetter` 已包含 `is_anonymous` 和 `student_read_at`，可支持匿名展示和回复红点。匿名记录在常规管理页面隐藏学生身份和联系方式，实名查看需要专门权限并记录日志。

---

## 2.4 书院简介

书院简介用于展示弘毅书院的基本介绍、文化理念、组织架构和联系方式。

页面内容由管理端通过富文本编辑器维护，微信端按富文本样式展示。

ORM 已补充 `ContentPage`，用于存储书院简介、服务说明、管理规范等富文本内容。

---

## 2.5 自习室预约

自习室预约用于学生和教师按日期、时间段选择自习室中的具体座位。

页面顶部提供两个切换入口：

1. 自习室预约
2. 我的预约

### 自习室预约列表

筛选条件：

- 日期
- 时间段

列表采用卡片形式展示自习室。

卡片展示内容：

- 自习室名称
- 位置
- 可预约座位数量
- 剩余可预约数量
- 环境说明（环境标签）
- 当前时间段可预约状态

点击卡片进入自习室预约详情页。

### 自习室预约详情

详情页展示自习室完整信息、当前选择的预约日期和时间段，以及按座位编号排列的选座区域。座位区明确区分“可选、已预约、已停用”状态。

用户选择一个可用座位后提交预约；提交成功后展示自习室、座位编号、日期和时间段。

预约规则：

- 同一用户在同一日期、同一时间段只能预约一个自习室中的一个座位，该规则对学生和教师统一生效。
- 同一座位在同一日期、同一时间段只能存在一条未取消预约；多人同时选择同一座位时，由数据库唯一约束保证只有一人成功，失败端刷新座位状态。
- 自习室可预约数量根据启用座位数和当前有效预约实时计算，不维护独立容量或余量库存表。
- 后台不允许创建时间上互相重叠的启用时间段，预约边界按 `[开始时间, 结束时间)` 处理。
- 管理端可临时关闭某个自习室的指定日期和时间段；已存在未来预约时，应先取消或调整相关预约并通知用户。
- 已进入预约时间段后，不允许取消预约。
- 未开始的预约可以取消。
- 学生信誉分低于后台设置的自习室预约最低信誉分时不允许预约；教师不执行学生信誉分限制。
- 系统读取学生在预约日期所属学年的 `StudentAcademicYearAbsence.study_room_absence_count`；达到后台设置的限制次数时不允许继续预约。教师缺席只记录本次预约状态，不累计学生缺席次数。

### 我的预约

展示当前用户的历史预约和未来预约。

预约状态：

- 已预约
- 已取消
- 缺席
- 已完成

对应 ORM：

- `StudyRoom`
- `StudyRoomSeat`
- `StudyRoomTimeSlot`
- `StudyRoomReservationSetting`
- `StudyRoomSlotClosure`
- `StudyRoomReservation`
- `StudentAcademicYearAbsence`

---

## 2.6 社区新闻

社区新闻用于展示书院发布的新闻内容。

### 新闻列表

导航筛选：

- 最热
- 最新
- 最多点赞
- 最多收藏
- 最多浏览

排序规则：

- 最新：按发布时间倒序。
- 最热：令 `V/L/F` 分别为最终浏览、点赞、收藏展示值，建议按 `(ln(1+V) + 3*ln(1+L) + 4*ln(1+F)) / (发布小时数+24)^0.35` 排序，压缩重复点击影响并避免旧新闻长期占据热榜。
- 最多点赞：按最终点赞展示值倒序。
- 最多收藏：按最终收藏展示值倒序。
- 最多浏览：按最终浏览展示值倒序。

最终展示值统一为 `max(0, 真实计数 + 人工修正值)`，所有榜单先展示置顶记录，再执行对应排序，确保列表顺序和用户看到的数字一致。浏览量本身仍然每次进入详情都加 1。

新闻卡片采用大长方形样式展示。

卡片展示内容：

- 封面图
- 标题
- 分类
- 浏览量
- 点赞量
- 收藏量
- 发布时间

### 新闻详情

详情页展示新闻富文本正文。

正文内容支持富文本，包含文字段落、图片、加粗、列表等图文混排内容。微信端根据 `News.content_format` 渲染 `News.content`。

可操作功能：

- 点赞
- 取消点赞
- 收藏新闻
- 取消收藏

用户每次进入新闻详情页时，系统都将浏览量原子加 1；不按用户、设备或时间去重，也不保存浏览明细。

对应 ORM：

- `News`
- `NewsCategory`
- `NewsLike`
- `NewsFavorite`

说明：

- 新闻正文使用 `News.content` 保存富文本内容。
- `News.content_format` 标记正文格式，默认建议使用 HTML 富文本；如果管理端编辑器输出结构化数据，可保存为编辑器 JSON。
- 学生和教师都可以点赞、收藏新闻，真实关系分别由 `NewsLike`、`NewsFavorite` 判断。
- `News.like_count`、`News.favorite_count`、`News.view_count` 保存真实计数；管理端人工调整保存到单独的修正值字段。
- 微信端展示数量为“真实计数 + 人工修正值”，最低展示为 0，避免人工调整覆盖真实互动数据。

---

## 2.7 活动

活动页用于展示书院发布的活动，并支持学生和教师报名。

### 活动列表

筛选项：

- 全部
- 待报名
- 报名中
- 待开始
- 进行中
- 已结束

卡片展示内容：

- 活动封面
- 活动标题
- 活动时间
- 活动分类
- 活动状态

活动封面图上只保留左上角状态角标，其他信息放在卡片正文区域。

### 活动状态

活动状态建议由时间规则驱动：

- 待报名：活动已发布，但当前时间早于报名开始时间。
- 报名中：当前时间处于报名开始时间至报名结束时间之间。
- 待开始：报名已结束，但当前时间早于活动开始时间。
- 进行中：当前时间处于活动开始时间至活动结束时间之间。
- 已结束：当前时间晚于活动结束时间。
- 已取消：管理端主动取消活动。

### 活动详情

详情页展示完整活动信息。

展示内容：

- 活动标题
- 活动封面
- 活动详情
- 主办方
- 活动地点
- 活动分类
- 报名开始和结束时间
- 活动开始和结束时间
- 人数上限
- 当前报名人数
- 德智体美劳分数

可操作功能：

- 报名
- 取消报名
- 查看报名状态

报名规则：

- 未到报名开始时间时不可报名。
- 超过报名结束时间后不可报名。
- 已满员时不可报名。
- 同一用户同一活动只能存在一个有效报名。
- 活动开始后不允许取消报名。
- 学生信誉分低于活动生效最低信誉分时不允许报名；教师不执行学生信誉分限制。
- 活动信誉分限制由活动配置决定：使用全局设置、单独设置或不限制。
- 学生活动开始日期所属学年的缺勤次数达到活动生效缺勤限制次数时不允许报名；教师缺勤只记录本次状态，不累计学生缺勤次数。
- 活动缺勤限制由活动配置决定：使用全局设置、单独设置或不限制。

### 活动分数

活动不设计“已到场”或“已确认参与”状态。管理端可直接对仍为“已报名”、未标记缺勤、尚未发分且绑定学生身份的报名记录发放德智体美劳分数；教师可以报名活动，但不发放学生五育分。

报名和取消必须在事务中锁定活动记录并原子维护报名人数；每次发分按报名记录和发分版本生成唯一业务键，防止并发重复发分。撤销时生成对应版本的反向流水，之后允许修正活动分值并按新版本重新发放。

活动配置的信誉分变动与五育分在同一次发分事务中生效；缺勤或违规的额外扣分使用单独流水记录。

活动已有当前有效发分时，不能直接修改活动分值、标记相关报名缺勤或取消活动。应先批量冲正有效分数，再修改、标记缺勤或取消；活动取消后保留报名和全部分数流水，且不得继续发分。

对应 ORM：

- `ActivityCategory`
- `ActivityParticipationSetting`
- `Activity`
- `ActivityRegistration`
- `StudentScoreRecord`
- `StudentAcademicYearAbsence`

---

## 2.8 广场

广场用于学生和教师发布帖子、浏览内容、参与评论和进行互动。

顶部切换：

- 广场：展示全部公开帖子。
- 关注：展示已关注用户发布的帖子。

### 广场首页

广场首页保留热门话题分类。

帖子卡片展示内容：

- 发布人头像
- 发布人姓名
- 文本内容，最多展示两行，超出省略
- 图片预览
- 话题标签
- 点赞量

点击帖子卡片进入帖子详情页，并将该帖浏览量原子加 1；每次进入都累加，不去重、不保存浏览明细。

### 话题详情

用户点击热门话题后，进入话题详情页。

话题详情页展示该话题下的帖子列表，帖子卡片样式与广场首页保持一致。

### 发布帖子

用户点击发布按钮后，可选择图片并输入文字。帖子只分为三种类型：

- 图片
- 文字
- 文字和图片

发布内容规则：

- 学生信誉分低于后台设置的广场发帖最低信誉分时不允许发帖；教师不执行学生信誉分限制。
- 广场发布不支持匿名。
- 图片或文字和图片帖子最多支持 8 张图片。
- 图片帖必须至少有 1 张图片；文字帖必须有文字且不能带图片；文字和图片帖必须同时包含文字及 1～8 张图片。
- 话题标签由管理端创建，学生和教师只能选择已有标签。
- 发布前需要勾选并同意《弘毅书院内容管理规范》。

### 帖子详情

详情页展示完整帖子内容、图片、评论列表和底部互动栏。

展示内容：

- 发布人头像
- 发布人姓名
- 关注按钮
- 文本内容
- 图片，支持多图左右滑动查看
- 评论列表

可操作功能：

- 点赞帖子
- 取消点赞
- 收藏帖子
- 取消收藏
- 评论帖子
- 举报帖子
- 回复评论
- 点赞评论
- 举报评论
- 关注发布者
- 点击发布者头像进入发布者信息页

评论支持继续回复；评论不支持收藏。用户举报帖子或评论后，举报记录进入 PC 后台举报受理列表。

发布者信息页是公开资料页，只展示头像、姓名、学生/教师身份、所属学院、公开帖子以及关注统计。学号、工号、手机号、专业、班级和宿舍等私人资料只在用户自己的“我的”页面或授权管理端展示，公开页不能复用完整个人资料接口。

对应 ORM：

- `PlazaTopicTag`
- `PlazaPostSetting`
- `PlazaPost`
- `PlazaPostTag`
- `PlazaPostImage`
- `PlazaPostLike`
- `PlazaPostFavorite`
- `PlazaComment`
- `PlazaCommentLike`
- `UserFollow`
- `ContentReport`

说明：ORM 中 `PlazaPost` 不设计匿名字段，符合“广场发布不允许匿名”的产品规则。

---

## 2.9 消息

“消息”是独立的底部 tab，用于聚合与当前用户相关的业务提醒和管理待办，不再放在“我的”页面中。

学生用户消息示例：

- 已报名活动即将开始提醒。
- 参加活动后的分数到账提醒。
- 会议室预约审批通过、拒绝或审核超时提醒。
- 自习室缺席提醒。

教师或管理人员消息示例：

- 负责心灵树洞回复时，收到新的树洞来信提醒。
- 负责领导信箱回复时，收到新的信件提醒。
- 负责会议室审批时，收到新的会议室预约审批提醒。
- 负责自习室管理时，收到需要处理的缺席或违规提醒。
- 学生导员收到宿舍查寝反馈提醒。

消息根据当前用户身份、角色和管理职责动态生成。tab 图标在存在未读消息或未完成待办时显示红点；用户进入消息详情后将对应消息标记为已读。同一业务待办发给多人时共享 `todo_key`，任一处理人完成业务后，所有接收人的同一待办统一完成，但每个人的已读状态仍独立维护。

对应 ORM：`Notification`

---

## 2.10 我的

“我的”页面用于展示当前用户的个人信息、分数、广告、个人业务入口和管理服务，不再承载消息列表。

### “我的”tab页广告位

“我的”tab 页广告位展示 PC 管理端已启用的广告，每条广告只展示一张图片，不叠加广告名称等管理信息；没有启用广告时隐藏整个区域。

点击广告后，根据配置三选一跳转：

- 本程序活动：打开对应活动详情页。
- 本程序广场帖子：打开对应帖子详情页。
- 外部小程序：使用配置的 AppID、页面路径和版本打开；页面路径为空时进入其首页。

广告按后台排序值升序展示。同一广告只允许配置一种跳转目标；目标不存在、已删除、已隐藏或当前不可访问时不下发该广告。

对应 ORM：`Advertisement`

### 个人信息

展示内容：

- 头像
- 姓名
- 学号/工号
- 学院
- 专业
- 班级
- 宿舍
- 手机号

用户点击顶部个人信息区域，可进入个人信息详情页。

学生展示学院、专业、班级和宿舍；教师展示所属学院、职称和职务，不要求教师具有专业、班级或宿舍数据。

数据来源：

- `User`
- `Student`
- `Teacher`
- `College`
- `Major`
- `SchoolClass`
- `Dormitory`

### 我的分数

学生端展示终身累计的当前德智体美劳分数和信誉分；教师没有学生五育档案时不展示“我的分数”区域。

展示规则：

- 默认只展示“五育综合”。
- 用户点击展开后，展示德、智、体、美、劳各项终身累计分数、信誉分，以及当前学年的自习室缺席次数和活动缺勤次数。
- 分数变动记录可进入单独页面查看。
- 按学期或学年查看得分时，根据分数流水发生时间筛选统计，不对学生累计总分执行清零。

数据来源：

- `Student`
- `StudentScoreRecord`
- `StudentAcademicYearAbsence`

### 我的业务

入口包括：

- 我的活动
- 我的帖子
- 我的收藏
- 个人信息

四个入口正好一行展示。

### 管理服务

当用户具备微信管理端权限时，在“我的”页面展示“管理服务”区域。

管理服务根据 `Role` 和 `MenuWeixin` 动态展示。没有微信管理权限的学生不展示该区域。

---

## 2.11 会议室预约

会议室预约用于具备后台授权角色的用户提交会议室使用申请。

申请权限规则：

- 后台配置允许申请会议室的角色。
- 当前用户没有任一允许角色时，不展示或不可提交会议室预约申请。

### 会议室列表

筛选条件：

- 日期
- 时间段
- 可容纳人数

卡片展示内容：

- 会议室名称
- 位置
- 容纳人数
- 设备说明
- 可申请仪器说明
- 可预约状态

### 预约申请

申请内容：

- 会议主题
- 用途说明
- 预约日期
- 时间段
- 参会人数
- 是否使用仪器
- 使用仪器种类
- 仪器使用说明
- 联系人
- 联系电话

提交后进入“待审核”状态，并生成审核截止时间。

待审核和已通过的申请都会占用对应会议室时段，拒绝、取消或审核超时后释放；同一用户同一日期和时间段只能存在一个待审核或已通过申请，参会人数不能超过会议室容量。待审核超过后台配置的有效时长后自动变为“审核超时”，查询可用时段和提交新申请时也会即时清理对应过期占位。后台不能创建时间上互相重叠的启用时间段。

取消规则：待审核申请可在会议开始前取消；已通过申请只能在会议开始前达到后台配置的提前分钟数时取消。管理人员可以强制取消，但必须填写原因并记录操作日志。

仪器选择规则：

- 仪器种类由后台维护。
- 选择“使用仪器”后，至少选择一种仪器。
- 未选择“使用仪器”时，不展示或不提交仪器种类。
- 仪器只记录勾选的种类，不管理库存和申请数量。

### 我的会议室预约

展示当前用户提交的会议室预约记录。

状态包括：

- 待审核
- 已通过
- 已拒绝
- 已取消
- 审核超时
- 已完成

对应 ORM：

- `MeetingRoom`
- `MeetingRoomTimeSlot`
- `MeetingRoomEquipment`
- `MeetingRoomReservationSetting`
- `MeetingRoomReservation`
- `MeetingRoomReservationEquipment`

## 3. PC 管理端功能设计

## 3.1 登录与权限

管理端用户登录后，根据角色加载菜单和操作权限。

基础功能包括：

- 用户管理
- 角色管理
- PC 菜单管理
- 微信菜单权限管理
- 权限分配

角色管理规则：

- 新增角色时，可绑定对应的 PC 管理端菜单。
- 新增角色时，可绑定对应的微信管理端菜单。
- 菜单权限只控制入口展示，不自动授予该菜单下的内容管理权限。
- 新增、编辑、删除、审核、回复、导出等接口操作通过 Django `Permission` 显式分配；领导信箱等业务还要校验收件方数据范围。
- 角色停用、撤销或权限变更后立即生效，并记录操作人、变化前后内容和操作时间。

对应 ORM：

- `User`
- `Role`
- `Menu`
- `MenuWeixin`
- Django `Permission`

---

## 3.2 管理人员权限分配

系统基础角色由开发人员预先配置。

平台管理人员在分配管理人员权限时，通过“人员职务分配”选择用户并绑定角色。用户获得角色后，即可拥有该角色对应的 PC 管理端菜单和微信管理端服务入口。

---

## 3.3 学校基础数据管理

管理端维护学校基础数据。

功能包括：

- 学院管理
- 专业管理
- 班级管理
- 宿舍管理
- 教师管理
- 学生管理
- Excel 一键导入

Excel 导入规则：

1. 识别或创建学院。
2. 识别或创建专业。
3. 识别或创建教师。
4. 识别或创建班级。
5. 识别或创建宿舍。
6. 识别或创建学生。

学生只保存班级关联，专业和学院通过班级推导。每次导入生成导入批次，记录文件、操作人、总行数、成功数和失败数；逐行失败原因写入独立错误记录，并生成可下载的错误 Excel，避免把大量错误塞入一个 JSON 字段。

对应 ORM：

- `College`
- `Major`
- `SchoolClass`
- `Dormitory`
- `Teacher`
- `Student`
- `SchoolImportBatch`
- `SchoolImportError`

---

## 3.4 内容发布管理

### 书院简介管理

管理端通过富文本编辑器维护书院简介。

功能包括：

- 编辑简介内容
- 上传或插入图片
- 预览微信端效果
- 保存发布

ORM 已补充通用内容页模型 `ContentPage`。

### “我的”tab页广告管理

“我的”tab 页广告由 PC 管理端在内容发布工作区统一维护。

功能包括：

- 广告列表、名称搜索和启用状态筛选
- 新增、编辑和软删除广告
- 上传并预览单张广告图片
- 配置广告排序
- 选择跳转类型：本程序活动、本程序广场帖子或外部小程序
- 选择本程序活动或帖子，并在保存、启用时校验目标是否有效
- 配置外部小程序 AppID、页面路径和版本
- 启用或停用广告，并预览微信端展示和跳转目标

编辑表单根据跳转类型动态展示字段：选择本程序活动或帖子时只显示对应内容选择器；选择外部小程序时只显示 AppID、页面路径和版本。启用广告前必须已有图片和有效跳转目标。停用后记录保留，但微信端立即不再展示。

对应 ORM：

- `Advertisement`

---

## 3.5 领导信箱管理

管理端查看和回复学生来信。

功能包括：

- 信件列表
- 按状态筛选
- 按学生、标题、分类搜索
- 按收件领导/部门筛选，并按收件方配置的数据范围限制可见记录
- 查看详情
- 回复信件
- 支持excel导出
- 信箱分类新增、编辑、排序、启用和停用
- 收件领导/部门新增、编辑、排序、启用和停用
- 为每个收件方配置可处理角色；角色变更后立即影响来信数据范围和待办接收人

停用分类或收件方后，不再用于新信件，但历史信件仍保留原分类和收件方并可正常查看。

对应 ORM：

- `LeaderMailboxCategory`
- `LeaderMailboxReceiver`
- `LeaderMailboxLetter`

---

## 3.6 心灵树洞管理

管理端查看和回复心灵树洞内容。

功能包括：

- 树洞列表
- 按状态筛选
- 按学生、标题搜索
- 查看详情
- 回复内容
- 支持excel导出

对应 ORM：

- `TreeHoleLetter`

---

## 3.7 自习室管理

功能包括：

- 自习室时间段管理
- 自习室管理
- 自习室座位编号、排序、启用和停用管理
- 指定日期、时间段的自习室临时关闭管理
- 预约记录管理
- 缺席记录管理
- 违规处理
- 查看某日期、某时间段、某自习室的预约用户列表
- 标记预约用户缺席
- 对缺席学生一键发送提醒
- 设置缺席限制次数
- 设置自习室预约最低信誉分
- excel数据导出（某个自习室在某月/年的使用人数）

管理端可维护自习室名称、位置、备注和启用状态，以及每个自习室的座位编号。可预约容量直接取启用座位数量，不单独填写容量；停用仍有未来预约的座位前，必须先取消或调整相关预约。

对应 ORM：

- `StudyRoom`
- `StudyRoomSeat`
- `StudyRoomTimeSlot`
- `StudyRoomReservationSetting`
- `StudyRoomSlotClosure`
- `StudyRoomReservation`

---

## 3.8 会议室管理

功能包括：

- 会议室时间段管理
- 会议室管理
- 会议室仪器种类管理
- 会议室可申请角色设置
- 待审核有效时长和已通过申请取消提前量设置
- 预约申请审核
- 预约记录查询
- 取消或完成预约

审核结果包括通过和拒绝。拒绝时需要填写原因；超过审核截止时间的申请自动变为“审核超时”并释放时段。管理端强制取消申请时同样需要填写原因。

对应 ORM：

- `MeetingRoom`
- `MeetingRoomTimeSlot`
- `MeetingRoomEquipment`
- `MeetingRoomReservationSetting`
- `MeetingRoomReservation`
- `MeetingRoomReservationEquipment`

---

## 3.9 广场管理

功能包括：

- 话题标签管理
- 帖子列表
- 评论列表
- 帖子隐藏
- 评论隐藏
- 图片查看
- 点赞、收藏、评论数据查看
- 查看帖子和评论关联举报
- 广场发帖最低信誉分设置
- 内容管理规范维护

话题标签由管理端创建，微信端学生只允许选择已有标签。

对应 ORM：

- `PlazaTopicTag`
- `PlazaPostSetting`
- `PlazaPost`
- `PlazaComment`
- `ContentReport`

---

## 3.10 活动管理

功能包括：

- 活动分类管理
- 活动参与全局最低信誉分设置
- 活动参与全局缺勤限制次数设置
- 活动新增
- 活动编辑
- 活动发布
- 活动取消
- 报名名单查看
- 缺勤标记与撤销
- 分数发放
- 分数批量冲正和重新发放
- excel数据导出（某月或年的活动参与人数情况）

活动配置内容：

- 活动标题
- 活动分类
- 封面图
- 活动详情
- 主办方
- 活动地点
- 报名时间
- 活动时间
- 人数上限
- 德智体美劳分数
- 信誉分变动
- 活动报名信誉分限制模式
- 活动最低信誉分
- 活动缺勤限制模式
- 活动缺勤限制次数

报名名单只对具有活动管理权限的人员开放，不在学生或教师的普通活动详情页展示。活动不设置到场确认；管理人员直接对符合发分条件的学生报名记录发放分数。存在当前有效发分时，不允许直接修改活动分值、把已发分用户标记缺勤或取消活动，必须先完成批量冲正。

对应 ORM：

- `ActivityCategory`
- `ActivityParticipationSetting`
- `Activity`
- `ActivityRegistration`
- `StudentScoreRecord`

---

## 3.11 新闻管理

新闻通过富文本编辑器编写正文。

功能包括：

- 新闻分类管理
- 新闻新增
- 新闻编辑
- 富文本正文编辑
- 正文图片上传与插入
- 新闻发布
- 新闻下架
- 置顶和排序
- 点赞量、收藏量、浏览量真实计数查看
- 点赞量、收藏量、浏览量人工修正值调整

说明：

- 新闻正文保存到 `News.content`，正文格式由 `News.content_format` 标记。
- 列表封面图使用 `News.cover_url`，正文图片写入富文本内容，不与封面图混用。
- `News.like_count`、`News.favorite_count`、`News.view_count` 保存真实计数，管理端不能直接覆盖。
- 管理端调整对应 `*_count_adjustment` 修正值，微信端展示“真实计数 + 修正值”，最低为 0。
- 真实点赞、收藏关系仍分别由 `NewsLike`、`NewsFavorite` 保存。
- 浏览量每次进入详情直接加 1，不去重、不保存浏览明细。
- 最多浏览、最多点赞、最多收藏和最热榜均使用最终展示值排序；最热榜对数量取对数并加入发布时间衰减。

对应 ORM：

- `NewsCategory`
- `News`
- `NewsLike`
- `NewsFavorite`

---

## 3.12 举报受理管理

PC 管理端统一受理微信端提交的内容举报。

举报来源包括：

- 广场帖子举报
- 广场评论举报

列表筛选项：

- 全部
- 待受理
- 处理中
- 已处理
- 不予受理

列表展示内容：

- 举报对象类型
- 举报原因
- 举报说明
- 举报人
- 被举报用户
- 举报时间
- 当前处理状态

详情页展示内容：

- 举报原因和说明
- 被举报内容快照
- 举报人信息
- 被举报用户信息
- 原内容跳转入口
- 历史处理记录

可操作功能：

- 标记处理中
- 不予受理
- 隐藏被举报内容
- 警告用户
- 限制用户
- 填写处理说明

同一用户对同一对象只能存在一条待受理或处理中的举报；已有举报处理完成或不予受理后，允许再次举报。每次状态变更和处理动作都写入不可删除、不可编辑的处理日志。同一次受理可以同时执行隐藏内容、警告用户、限制用户等多个动作。

限制用户时必须明确限制的功能、开始时间、结束时间和原因，不使用含义过宽的整号禁用代替功能限制。结束时间必须晚于开始时间，同一用户同类限制的有效时间不能重叠；限制记录不能删除，只能记录撤销时间和撤销人。

对应 ORM：

- `ContentReport`
- `ContentReportActionLog`
- `UserRestriction`

---

## 4. 微信管理端功能设计

微信管理端功能显示在“我的”页面的“管理服务”区域。

展示规则：

- 后台通过角色为用户分配微信管理权限。
- 当前用户拥有对应 `MenuWeixin` 权限时，才展示对应管理入口。
- 学生用户默认不展示管理服务。

## 4.1 微信活动管理

微信活动管理用于活动现场或移动端快速处理参与人员。

功能包括：

- 查看某个活动的参与人员。
- 标记报名用户缺勤。
- 对缺勤或违规学生扣减信誉分；教师只记录本次缺勤状态。
- 对仍为已报名、未缺勤、未发分且绑定学生身份的记录发放活动分数。

规则：

- 被标记缺勤的学生不发放本次活动五育分。
- 管理端标记或撤销活动缺勤时，报名人为学生才按活动开始日期归属学年，同步维护 `StudentAcademicYearAbsence.activity_absence_count`；教师不累计学生缺勤次数。
- 分数发放后需要写入 `StudentScoreRecord`。
- 已发放分数的报名记录应标记为已发放并记录发分版本，避免重复发放；已发分记录必须先冲正才能标记缺勤。
- 不设置到场或参与确认状态；报名名单仅对具有活动管理权限的人员开放。

## 4.2 会议室预约审核

当用户负责会议室审批时，“消息”tab 中展示新的会议室预约待办。

功能包括：

- 查看预约申请详情。
- 审核通过。
- 审核拒绝并填写原因。
- 查看申请人是否使用仪器以及选择的仪器种类。

超过审核截止时间的申请不能再审核，待办随申请变为“审核超时”而统一完成。同一申请待办发给多人时，任一人完成审核后，其他人的相同待办同时完成。

## 4.3 自习室管理

自习室管理用于查看预约情况和处理违规预约。

功能包括：

- 查看指定日期、时间段、自习室下的预约人员。
- 查看预约人信息。
- 标记预约人缺席。
- 对缺席学生一键发送提醒。
- 对违规人员扣减信誉分。
- 管理端标记或撤销自习室缺席时，需要按预约日期归属学年，同步维护 `StudentAcademicYearAbsence.study_room_absence_count`。

## 4.4 领导信箱回复

当用户负责领导信箱回复时，“消息”tab 中展示新的来信待办。

功能包括：

- 查看来信详情。
- 回复来信。

同一来信待办可能发给多个有处理权限的用户；任一人回复后，其他人的相同待办统一完成。

## 4.5 心灵树洞回复

当用户负责心灵树洞回复时，“消息”tab 中展示新的树洞来信待办。

功能包括：

- 查看树洞内容。
- 回复树洞内容。

同一树洞待办可能发给多个有处理权限的用户；任一人回复后，其他人的相同待办统一完成。

## 4.6 宿舍管理

宿舍管理用于移动端快速查看宿舍和学生住宿信息。

功能包括：

- 查看宿舍楼。
- 查看宿舍房间。
- 查看宿舍内学生信息。
- 对学生提交宿舍查寝反馈。
- 反馈提交后，系统在该学生导员的“消息”tab 中生成提醒。

---

## 5. 核心业务规则

## 5.1 用户与身份

- 微信端用户通过 `openid` 识别身份。
- 首次登录用户为 `unbound`，系统随机生成不可编辑、不展示的 `username`；绑定完成前不能使用普通业务。账号启停统一使用 Django 自带的 `is_active`。
- `User` 只能绑定 `Student` 或 `Teacher` 其中一个，并与 `user_type` 一致。
- 学生和教师均可报名、发帖、评论、点赞、收藏、关注和预约自习室；这些通用关系关联 `User`。
- 信誉分和终身累计五育分保存在 `Student`；活动、自习室缺勤次数按学年保存在 `StudentAcademicYearAbsence`，都只对绑定学生身份的用户生效。
- 身份绑定使用可靠校验凭据并限制失败次数；解绑和换绑需要管理员授权及操作日志。
- 广场公开作者资料与“我的资料”使用不同返回结构，公开页不得返回学号、工号、手机号、专业、班级或宿舍；系统不保存明文身份证号。

## 5.2 权限控制

- `Menu`、`MenuWeixin` 只控制 PC 或微信端入口展示，不能代替接口授权。
- PC 和微信管理接口统一校验 Django `Permission`；领导信箱等业务继续校验收件方数据范围。
- 普通业务入口对学生和教师开放，管理服务入口只对具备角色和操作权限的用户开放。
- 角色分配、撤销和权限变化必须记录操作日志，并在变更后立即生效。

## 5.3 软删除

业务表默认使用软删除。

- 普通查询只返回未删除数据。
- 管理端可根据需要提供恢复或彻底删除能力。
- MySQL 不使用条件唯一约束：唯一业务字段与 `unique_alive_token` 组合；未删除为 `alive`，软删除改为记录专属 UUID。
- 取消、拒绝或已处理等释放状态将 `unique_active_token` 置为 `NULL`；启用配置使用 `unique_enabled_token`，停用时置为 `NULL`。
- 点赞、收藏、报名、预约等关系记录必须通过业务服务软删除，并在同一事务内同步计数或资源占用状态。
- 恢复数据前检查唯一约束冲突；查询子记录时同时确认父记录仍有效。
- 举报处理日志、用户限制记录和管理操作日志属于只追加审计数据，不提供普通编辑、软删除、恢复或彻底删除功能。

## 5.4 点赞、收藏和计数

点赞、收藏、报名、预约都使用独立关系表。

主表上的计数字段用于列表快速展示。

新增或删除关系记录时，需要在同一个事务里同步更新主表计数。

- 新闻点赞、收藏、浏览的真实计数与人工修正值分开保存；广场计数只保存真实值。
- 用户每次进入新闻或帖子详情页都将浏览量加 1，不去重、不保存浏览明细。
- 冗余计数使用数据库原子更新，并提供按关系表重新校准的管理能力。

## 5.5 红点与未读

领导信箱、心灵树洞和独立“消息”tab 都需要未读提示。

推荐规则：

- 管理端回复信件后，学生端列表显示红点。
- 学生进入详情页后，红点消失。
- 管理职责相关提醒进入“消息”tab，并按待办状态展示。
- 同一业务待办发给多人时共享 `todo_key`；业务完成后统一完成所有接收人的对应待办，已读状态仍按用户单独维护。

ORM 中信件表已补充 `student_read_at`，用于处理学生端回复红点。

## 5.6 分数与信誉分

学生档案中保存终身累计的当前德智体美劳分数和信誉分；自习室、活动缺勤次数按学年保存到 `StudentAcademicYearAbsence`。学年由学校配置的起始日计算，默认每年 9 月 1 日开始。

分数或信誉分变化需要写入流水，并带有唯一业务键防止重复处理。

活动分数发放时：

1. 在事务中锁定报名记录和学生记录，将 `score_grant_version` 加 1。
2. 使用“报名 ID + 发分版本”唯一业务键写入 `StudentScoreRecord`。
3. 更新 `Student` 终身累计分数。
4. 标记 `ActivityRegistration.score_granted=True`。

活动不设置到场或参与确认状态，只对“已报名、未缺勤、未发分”的学生报名记录发分。扣减信誉分也写入 `StudentScoreRecord`；撤销通过带发分版本的反向流水完成，不删除原流水，同一原流水最多冲正一次。活动存在有效发分时，修改分值、标记已发分报名缺勤或取消活动前必须先冲正。

## 5.7 行为准入与限制

- 学生和教师均可报名活动、预约自习室和在广场互动。
- 学生活动报名前需要校验信誉分，教师不执行学生信誉分校验。
- 活动信誉分限制可以使用全局设置、单活动单独设置或单活动不限制。
- 活动报名前需要读取活动开始日期所属学年的 `StudentAcademicYearAbsence.activity_absence_count`。
- 活动缺勤限制可以使用全局设置、单活动单独设置或单活动不限制。
- 学生活动开始日期所属学年的缺勤次数达到活动生效限制次数后，不允许报名活动。
- 学生自习室预约前需要校验信誉分，教师不执行学生信誉分校验。
- 自习室预约前直接读取预约日期所属学年的 `StudentAcademicYearAbsence.study_room_absence_count`，不再每次扫描预约历史。
- 学生预约日期所属学年的自习室缺席次数达到后台设置值后，不允许继续预约。
- 活动和自习室缺勤分别按活动开始日期、预约日期归属学年；报名和预约明细作为永久审计来源，撤销缺勤时同步递减原所属学年汇总。
- 会议室预约前需要校验当前用户是否拥有后台配置的可申请角色。
- 学生广场发帖前需要校验信誉分，低于后台设置值时不允许发帖；教师不执行该限制。

## 5.8 活动状态

数据库只保存草稿、已发布、已取消三种人工生命周期状态。待报名、报名中、待开始、进行中、已结束均根据北京时间实时计算，时间区间统一采用左闭右开规则；报名结束时间不得晚于活动开始时间。

## 5.9 内容安全

广场、评论、领导信箱和心灵树洞都涉及用户生成内容。

建议至少具备以下能力：

- 敏感词过滤。
- 管理端隐藏帖子或评论。
- 管理端查看图片内容。
- 广场帖子和评论支持举报，并由 PC 后台统一受理。
- 重要操作保留操作日志。
- 评论按两级展示，回复必须与根评论、被回复评论属于同一帖子。
- 顶级评论的回复量表示讨论串下全部可见回复数；回复本身不维护回复量，新增、隐藏和恢复回复时同步更新根评论。
- 同一用户对同一内容只能有一条待处理举报；处理结束后允许再次举报。举报处理日志不可删除，用户限制时间不得重叠且只能撤销不能删除。
- 领导信箱和树洞只允许提交者本人及有权限的管理人员查看；匿名记录的实名查看需要专门权限并记录日志。

## 5.10 “我的”tab页广告展示与跳转

- “我的”tab 页广告接口只返回未删除且已启用的 `Advertisement`，并按后台排序值升序返回。
- 每条广告只有一张图片和一个跳转目标，不支持同时配置多个目标。
- 跳转本程序活动或帖子前，接口必须确认目标存在且当前用户可访问；目标失效后广告不再下发。
- 跳转外部小程序时使用后台配置的 AppID、页面路径和版本；生产环境默认只投放正式版。
- 关闭广告开关后立即停止展示；停用与软删除是不同状态，停用记录可在 PC 管理端重新启用。

## 5.11 MySQL 与 Redis 运行约定

- 正式环境使用 MySQL 8.0+、InnoDB 和 `utf8mb4`；SQLite 仅用于本地快速验证，涉及并发、唯一索引和事务的测试必须使用 MySQL。
- MySQL 不支持 Django 条件唯一约束。软删除唯一性使用 `unique_alive_token` 组合索引；有效预约、有效报名和待处理举报使用可空的 `unique_active_token`，启用配置使用可空的 `unique_enabled_token`。可选唯一字段为空时统一存 `NULL`，不存空字符串。
- 自习室占座、活动报名、会议室申请、积分和缺勤计数由 MySQL 事务、行锁和唯一索引最终判定；Redis 只能缓存查询结果或保存临时状态，不能作为余量、报名人数或积分的最终来源。
- Redis 采用 cache-aside。首页聚合、新闻/活动列表、广场热门、自习室座位图、权限菜单和消息未读数可缓存；写 MySQL 成功后使用 `transaction.on_commit()` 失效相关 key。
- Redis 可用于登录/绑定/举报限流、幂等键结果、定时任务锁和防击穿短锁。Redis 故障时核心预约、报名、积分和审核业务仍须依赖 MySQL 正常工作。
- 状态字段和唯一标记必须在同一事务内更新，禁止用 `QuerySet.update()` 或 `bulk_update()` 单独改变状态而跳过唯一标记同步；软删除批量操作必须逐条生成删除 token。

---

## 6. ORM 对照与待补充点

## 6.1 已覆盖的 ORM

- 用户与权限：`User`、`Role`、`Menu`、`MenuWeixin`
- 学校基础数据：`College`、`Major`、`SchoolClass`、`Dormitory`、`Teacher`、`Student`、`StudentAcademicYearAbsence`、`DormitoryInspectionFeedback`、`SchoolImportBatch`、`SchoolImportError`
- 分数流水：`StudentScoreRecord`
- 信箱树洞：`LeaderMailboxCategory`、`LeaderMailboxReceiver`、`LeaderMailboxLetter`、`TreeHoleLetter`
- 自习室：`StudyRoom`、`StudyRoomSeat`、`StudyRoomTimeSlot`、`StudyRoomReservationSetting`、`StudyRoomSlotClosure`、`StudyRoomReservation`
- 会议室：`MeetingRoom`、`MeetingRoomTimeSlot`、`MeetingRoomEquipment`、`MeetingRoomReservationSetting`、`MeetingRoomReservation`、`MeetingRoomReservationEquipment`
- 广场：`PlazaTopicTag`、`PlazaPostSetting`、`PlazaPost`、`PlazaPostImage`、`PlazaPostLike`、`PlazaPostFavorite`、`PlazaComment`、`PlazaCommentLike`
- 活动：`ActivityCategory`、`ActivityParticipationSetting`、`Activity`、`ActivityRegistration`
- 新闻：`NewsCategory`、`News`、`NewsLike`、`NewsFavorite`
- 内容页：`ContentPage`
- “我的”tab 页广告：`Advertisement`
- 关注：`UserFollow`
- 消息 tab：`Notification`
- 举报受理：`ContentReport`、`ContentReportActionLog`、`UserRestriction`
- 管理审计：`OperationLog`

## 6.2 建议后续补充或确认

1. 初始化 PC、微信管理菜单和对应的 Django `Permission`，并为领导信箱收件方配置可处理角色。
2. 重要管理操作统一记录操作人、操作对象、操作前后变化和操作时间。
3. 正式环境使用 MySQL 8.0+、InnoDB、`utf8mb4`，Django 时区配置为 `Asia/Shanghai` 且保持 `USE_TZ=True`；并接入 Redis 作为缓存和临时状态服务。

---

## 7. 当前功能设计分析

## 7.1 整体判断

当前功能设计已经形成比较完整的“书院服务平台”框架：首页承接高频服务，活动模块连接报名和五育分，广场承担学生社区互动，独立“消息”tab 聚合提醒和待办，“我的”页面聚合身份、分数、广告和个人业务。

整体方向是成立的，且适合做成微信小程序。核心业务不是单纯展示内容，而是围绕学生在书院中的服务、参与、互动和成长记录形成闭环。

## 7.2 设计亮点

1. 首页聚合高频服务，能降低学生使用成本。
2. 活动与五育分打通，具备明确的业务价值。
3. 广场和关注形成轻量社区关系链，能提升学生之间的互动。
4. 独立“消息”tab 将学生提醒和教师待办合并为统一入口，未读信息更加醒目。
5. PC 管理端和微信管理端分工清晰，PC 负责完整管理，微信端负责轻量处理和现场操作。

## 7.3 当前主要风险

1. 微信管理端依赖角色和 `MenuWeixin`，但还需要明确每个管理入口的权限编码和初始化数据。
2. 活动状态必须由时间规则兜底，不能只依赖人工维护状态，否则首页推荐容易出现“已经无法报名但仍被推荐”的问题。
3. 广场帖子和评论属于用户生成内容，正式上线前需要完善内容审核、举报受理和隐藏机制。
4. 分数和信誉分会影响学生权益，所有加分、扣分、撤销都需要可追溯记录。

## 7.4 建议开发优先级

第一阶段建议先完成身份绑定、首页、活动、新闻、自习室预约、会议室预约和“我的”基础能力。这些是小程序的服务主干。

第二阶段完成领导信箱、心灵树洞、广场帖子、评论、点赞、收藏和个人业务页面。这一阶段把互动和反馈能力补齐。

第三阶段完成关注、消息 tab 和微信管理端。这些功能依赖关系更复杂，建议在基础业务稳定后再联调。

第四阶段完善 PC 管理端权限、数据导入、内容审核、操作日志和统计能力，为正式使用做支撑。
